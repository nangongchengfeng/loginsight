## Problem Statement

当前 log-analyzer 工具的 stats 命令只展示了基础指标，缺少一些能帮助用户更深入理解访问模式和问题的关键统计维度。用户希望看到更丰富的分析视角。

## Solution

在 stats 命令中新增四个实用指标，增强对 Nginx 日志的分析能力：按小时的时间分布、Top 10 具体状态码、唯一 IP 总数、以及错误率指标。

## User Stories

1. 作为运维人员，我想看到按小时的请求分布，以便识别每日流量高峰低谷期
2. 作为运维人员，我想看到 Top 10 具体状态码，以便快速定位最常见的错误（如 404、500）
3. 作为运维人员，我想知道唯一 IP 总数，以便了解活跃访问者规模
4. 作为运维人员，我想看到 4xx/5xx 错误率百分比，以便直观评估服务稳定性
5. 作为运维人员，我想在一个视图中同时看到所有这些指标，无需切换多个工具

## Implementation Decisions

### 模块修改

**analyzer 模块**：
- 扩展 `StatsResult` 结构体，新增字段存储时间分布、唯一 IP 数
- 修改 `Analyze` 函数，增加按小时聚合时间、统计唯一 IP 数、计算错误率的逻辑
- 修改 `PrintStats` 函数，调整输出顺序，新增各指标的渲染方法
- 新增辅助函数：将状态码 map 转换为可排序结构、计算百分比等

### 输出顺序调整

新的输出顺序为：
1. 总请求数卡片（保留）
2. 状态码分类分布 + Top 10 具体状态码（增强）
3. 错误率指标（新增）
4. 时间分布（新增）
5. IP 统计（唯一 IP 数 + Top 10 IP，增强）
6. Top 10 URL（保留）
7. Top 10 User-Agent（保留）

### 关键设计

- 时间分布按小时聚合（00-23），以本地时区为准
- 错误率计算：4xx 率 = 4xx 请求数 / 总请求数，5xx 率类似
- 状态码展示：先展示 2xx/3xx/4xx/5xx 分类分布，再展示 Top 10 具体状态码
- 唯一 IP 数直接展示在 Top 10 IP 表格的上方

## Testing Decisions

### 测试策略

- 测试 analyzer 模块的外部行为，不测试内部实现细节
- 验证新增指标的计算正确性，不测试 UI 渲染
- 保持与现有测试相同的风格和结构

### 测试覆盖

- analyzer 模块的 `Analyze` 函数需要新增测试用例，验证：
  - 时间分布正确按小时聚合
  - 唯一 IP 数计算正确
  - 状态码统计包含具体值
  - 错误率计算正确
- 参考现有测试文件 `analyzer_test.go` 的模式

## Out of Scope

- 流量/字节数相关统计
- Referer 字段分析
- User-Agent 详细解析（浏览器/设备类型识别）
- URL 路径分类统计
- 交互式图表或可视化输出
- 配置文件支持自定义指标展示

## Further Notes

- 所有新增指标都基于现有的 `parser.LogEntry` 结构，无需修改解析逻辑
- 保持向后兼容，不影响现有 filter 和 gen-sample 命令
- 优先考虑性能，对于大文件统计保持可接受的处理速度
